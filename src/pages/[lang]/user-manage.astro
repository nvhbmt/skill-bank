---
import { useTranslate } from '@/i18n';

// Trang này từng là bản mockup tĩnh với dữ liệu giả. Chức năng quản lý người
// dùng thật đã nằm ở /admin (tab "Người dùng"), nên chuyển hướng sang đó.
const { l } = useTranslate(Astro);

return Astro.redirect(l('/admin'), 302);
---
