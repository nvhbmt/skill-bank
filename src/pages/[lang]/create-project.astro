---
import '@/assets/css/create-project.css';
import Header from '@/components/Header.astro';
import SkillDetailDialog from '@/components/SkillDetailDialog.astro';
import ToastDialog from '@/components/ToastDialog.astro';
import ProjectForm from '@/components/ProjectForm.astro';
import { useTranslate } from '@/i18n';
import DefaultLayout from '@/layouts/DefaultLayout.astro';
import { getAllSkills } from '@/services/skills';

const { t, l } = useTranslate(Astro);
const session = Astro.locals.session;
if (!session?.user) {
    return Astro.redirect(l(`/sign-in`));
}

// Đổ sẵn danh sách kỹ năng vào select thay vì để client tự gọi /api/skills
const allSkills = await getAllSkills();
---

<DefaultLayout>
    <div class="wrapper">
        <Header />

        <main>
            <div class="hero-section">
                <h1>{t('create-project.title')}</h1>

                <ProjectForm mode="create" allSkills={allSkills} />
            </div>
        </main>
    </div>

    <SkillDetailDialog skills={[]} />
    <!-- JustValidate chỉ cần ở các trang có form kiểm tra dữ liệu.
        Trước đây nạp trong DefaultLayout nên 29KB này tải trên mọi trang. -->
    <script
        is:inline
        src="https://unpkg.com/just-validate@4.3.0/dist/just-validate.production.min.js"
    ></script>
    <script src="@/assets/script/create-project.js"></script>

    <ToastDialog />
</DefaultLayout>
